apiVersion: redhatcop.redhat.io/v1alpha1
kind: SecretEngineMount
metadata:
  name: transit
  namespace: vault-admin
spec:
  authentication:
    path: kubernetes
    role: policy-admin
  path: transit
  type: transit