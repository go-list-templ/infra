apiVersion: redhatcop.redhat.io/v1alpha1
kind: SecretEngineMount
metadata:
  name: kvv2-secret
spec:
  authentication:
    path: kubernetes
    role: policy-admin
  path: secret
  type: kv
  options:
    version: "2"