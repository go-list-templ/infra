apiVersion: redhatcop.redhat.io/v1alpha1
kind: Policy
metadata:
  name: policy
spec:
  authentication:
    path: kubernetes
    role: policy-admin
  policy: |
    path "secret/data/sso-service/*" {
      capabilities = ["read"]
    }