apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vault.fullname" . }}-tf-files
  namespace: {{ .Release.Namespace }}
data:
  # Мы динамически подгружаем содержимое всех файлов из вашей папки terraform
  {{- (.Files.Glob "terraform/*.tf").AsConfig | nindent 2 }}