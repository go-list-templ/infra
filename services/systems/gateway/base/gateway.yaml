apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gateway
  namespace: gateway
spec:
  gatewayClassName: nginx
  listeners:
    - protocol: HTTP
      name: http
      port: 80
      allowedRoutes:
        namespaces:
          from: All